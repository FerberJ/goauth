// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: verification.sql

package gen

import (
	"context"
)

const createVerification = `-- name: CreateVerification :one
INSERT INTO auth_verification (
    id, token_id, user_id, issued_at, expires_at, revoked
) VALUES (
    ?, ?, ?, ?, ?, FALSE
)
RETURNING id, token_id, user_id, issued_at, expires_at, revoked
`

type CreateVerificationParams struct {
	ID        string
	TokenID   string
	UserID    string
	IssuedAt  int64
	ExpiresAt int64
}

func (q *Queries) CreateVerification(ctx context.Context, arg CreateVerificationParams) (AuthVerification, error) {
	row := q.db.QueryRowContext(ctx, createVerification,
		arg.ID,
		arg.TokenID,
		arg.UserID,
		arg.IssuedAt,
		arg.ExpiresAt,
	)
	var i AuthVerification
	err := row.Scan(
		&i.ID,
		&i.TokenID,
		&i.UserID,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.Revoked,
	)
	return i, err
}

const getVerification = `-- name: GetVerification :one
SELECT id, token_id, user_id, issued_at, expires_at, revoked FROM auth_verification
WHERE token_id = ? LIMIT 1
`

func (q *Queries) GetVerification(ctx context.Context, tokenID string) (AuthVerification, error) {
	row := q.db.QueryRowContext(ctx, getVerification, tokenID)
	var i AuthVerification
	err := row.Scan(
		&i.ID,
		&i.TokenID,
		&i.UserID,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.Revoked,
	)
	return i, err
}

const verificationExists = `-- name: VerificationExists :one
SELECT EXISTS(
    SELECT 1 FROM auth_verification WHERE token_id = ?
) AS token_exists
`

func (q *Queries) VerificationExists(ctx context.Context, tokenID string) (bool, error) {
	row := q.db.QueryRowContext(ctx, verificationExists, tokenID)
	var token_exists bool
	err := row.Scan(&token_exists)
	return token_exists, err
}

const verificationIDExists = `-- name: VerificationIDExists :one
SELECT EXISTS(
    SELECT 1 FROM auth_verification WHERE id = ?
) AS id_exists
`

func (q *Queries) VerificationIDExists(ctx context.Context, id string) (bool, error) {
	row := q.db.QueryRowContext(ctx, verificationIDExists, id)
	var id_exists bool
	err := row.Scan(&id_exists)
	return id_exists, err
}

const verificationRevoke = `-- name: VerificationRevoke :exec
UPDATE auth_verification
SET
    revoked = TRUE
WHERE id = ?
`

func (q *Queries) VerificationRevoke(ctx context.Context, id string) error {
	_, err := q.db.ExecContext(ctx, verificationRevoke, id)
	return err
}
